mod examples;

pub fn transfer(from: String, to: String, amount: uint64) {
   // read file transfer.rho
   // get first line of rholang
   // parse out what's in the [square brackets]
   // replace from left to right with arguments passed in, checking for type on the way
   // read or create key configuration
   // invoke deploy/propose or easyDeploy
   // print response
}
